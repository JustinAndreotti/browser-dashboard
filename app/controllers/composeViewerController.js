export async function getServiceComposeFile(serviceName) {
  console.log(`inside getServiceComposeFile function for the service: ${serviceName}`)

  //TODO:
  //get the path to the compose.yaml file
  //fs in the compose.yaml and turn it into plain text
}